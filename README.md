# claude-multi

**This project is archived.**

Since `@anthropic-ai/claude-code` v2.1.113, the npm version requires the native Claude CLI to be installed. This tool only supported the npm-based `claude` command and no longer works as expected.

Please use [felix021/claude-env](https://github.com/felix021/claude-env) instead, which supports the native Claude CLI.
